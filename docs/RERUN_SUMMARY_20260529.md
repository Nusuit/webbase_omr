# Redmi Web Rerun Summary

Date: 2026-05-29

Device: Redmi Note 13 Pro+ (`23090RA98G`) over USB debugging.
Browser: Android Chrome `148.0.7778.178`.
Server: local `batch-detect.html` through `adb reverse tcp:8080 tcp:8080`.
Control: Chrome DevTools Protocol through `adb forward tcp:9222 localabstract:chrome_devtools_remote`.

## Artifacts

| Run | Batch JSON | Resource CSV | Log |
|---|---|---|---|
| Web CV | `runs/batch_results/redmi_note13_pro_plus_20260529_cv_n179.json` | `runs/resources/redmi_note13_pro_plus_20260529/resources_redmi_note13_pro_plus_20260529_cv_n179.csv` | `logs/drive_redmi_note13_pro_plus_20260529_cv.log` |
| Web YOLO raw handoff | `runs/batch_results/redmi_note13_pro_plus_20260529_yolo_raw_n179.json` | `runs/resources/redmi_note13_pro_plus_20260529/resources_redmi_note13_pro_plus_20260529_yolo_raw_n179.csv` | `logs/drive_redmi_note13_pro_plus_20260529_yolo_raw.log` |
| YOLO detection summary | `runs/accuracy_eval/yolo_detection_20260529_n179/summary.json` | `runs/accuracy_eval/yolo_detection_20260529_n179/per_sheet_detection.csv` | generated by `scripts/summarize_yolo_detection.py` |

The `_sys.csv` files were also written, but desktop `psutil` cannot see Android
Chrome renderer PIDs through ADB forwarding, so those files contain zero CPU/RAM
samples and should not be used for Android resource claims.

## Timing Summary

| Run | N | `cpp_ms` | `yolo_ms` | `worker_total_ms` |
|---|---:|---:|---:|---:|
| Web CV 2026-05-29 | 179 | `795.2 +/- 118.7` ms | `0.0 +/- 0.0` ms | `795.4 +/- 118.7` ms |
| Web YOLO raw 2026-05-29 | 179 | `820.1 +/- 75.5` ms | `309.4 +/- 39.0` ms | `1130.1 +/- 89.0` ms |

For comparison, the current paper artifacts report:

| Run | N | `cpp_ms` | `yolo_ms` | `worker_total_ms` |
|---|---:|---:|---:|---:|
| Web CV original | 179 | `807.8 +/- 234.3` ms | n/a | n/a |
| Web YOLO raw original | 179 | `648.0 +/- 49.2` ms | `298.7 +/- 62.0` ms | `947.2 +/- 88.3` ms |

The CV rerun is close to the original mean with a smaller SD. The YOLO raw
rerun is materially slower in the WASM/CV portion and worker total, while YOLO
inference itself remains close to the original. Do not replace paper table
values with this rerun automatically; decide the canonical run after reviewing
thermal/session conditions.

## Detection/Handoff Summary

The 2026-05-29 YOLO raw rerun matches the earlier detection audit:

- `154/179` rows have `yolo_detected=true`.
- `179/179` rows use `yolo_chosen_path=raw`.
- `0/179` rows use fallback.

This supports the raw-handoff completion claim, not a `179/179` detector-success
claim.

## In-Browser Resource Summary

| Run | Samples | CPU proxy mean/max | RAM mean/max |
|---|---:|---:|---:|
| Web CV 2026-05-29 | 1154 | `67.64% / 178.59%` | `46.94 / 156.42` MB |
| Web YOLO raw 2026-05-29 | 1580 | `58.93% / 177.60%` | `67.85 / 156.42` MB |

Resource values are browser-exposed/in-page proxies. They are suitable for
same-instrument Android Web sanity checks, not direct CPU-efficiency comparison
against desktop `psutil` or Native Android sampler rows.

## Remaining Work (correction 2026-05-30)

The Redmi device is connected, so the previous ADB blocker is gone. The earlier
claim that Native per-question export was blocked by a missing Android project is
**incorrect**: the complete buildable project lives in the sibling folder
`C:/Kien/Mobile/orm/android` (gradlew, `build.gradle.kts`, `settings.gradle.kts`,
keystore, 31 Kotlin files including `HeadlessBenchmark.kt`, `CvBenchmarkRunner.kt`,
`YoloBenchmarkRunner.kt`, `OmrProcessor.kt`). The earlier note only inspected the
empty `wasm-omr-mobile/android/` folder inside this repo, not the sibling project.

Headless batch already exists (`HeadlessBenchmark`, triggered via `BenchmarkActivity`
with `--es headless true --es method cv|yolo --es manifest <path>`). Per-question
data is already computed in `OmrProcessor` (`result.answers`) and used to derive the
`answered`/`multi` counts that the runners export. The remaining task is to also
serialize `auto_q01..auto_q60` in `HeadlessBenchmark.writeJson` (plus the matching
`SheetCapture` fields), rebuild via `cd ../android; .\gradlew.bat
assembleTraditionalDebug assembleYoloDebug`, run headless, pull the JSON, and
evaluate against the same reviewed ground truth.
